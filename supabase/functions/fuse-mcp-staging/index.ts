// Staging alias of fuse-mcp: identical code, separate URL, labelled "staging" in every response.
// Not linked from the site. Delete this directory to retire the staging endpoint.
import "../fuse-mcp/index.ts";
